# RUN 00.9A — Condition Supersession

## Why `ck.m0.candidate.v1` failed scientifically

RUN 00.7 found the sole included task had gold equal to the full closed
universe, gold-visible/derivable controls, and C3 carrying output-ready gold.
The primary contrast could not isolate structured substrate continuity.

**Disposition:** RETIRED · NEVER RATIFY · NEVER OVERWRITE · hash `9ec3d37a…`.

## Superseding definitions (v2)

### C0 — Natural baseline

Episode-B instructions and query only. No prior accepted state.

### C1 — Flat state-mass control (**supersedes** prior “byte-matched bare only”)

Contains the same candidate state items and comparable non-answer informational
mass as C3, but **destroys** the mapping that identifies which relations were
accepted (deterministic noninformative permutation/assignment).  
Must **not** expose or determine the gold set.

Prior definitions of C1 as merely UTF-8-budget bare without state-mass content
are **superseded** for M0-v2 science (commissioning instrumentation may still
use older packet builders for non-scientific paths).

### C2 — Instruction and serialization control

Matches C3 operational instructions, schema, envelope, formatting, and
non-state metadata **without** accepted relational state.

### C3 — Structured verified continuity

Replay-derived structured representation of actually accepted Episode-A state.
Preferred: representation **distinct** from required output schema so the
model transforms state rather than copies scorer triples.

## Primary contrast

**C3 vs C1:** structured accepted-state mapping vs flat state-mass without that
mapping.

## Residual confounds (declared)

- Residual lexical differences not fully matched  
- Model-load order if not blocked  
- Single-replicate design (no independent replication claim)

## Valid components retained

Typed outcomes, scorer arithmetic, persistent ledger, publication gate,
commissioning evidence path, packet byte machinery (as instrument).
