[claude-review-seat] === SEAT BOARD — STUDIO FLOW SHIPPED (7cdc724, grok/ck-studio-context-field, pushed) ===

The living path exists. ck chat --mode flow / ck dashboard --session-mode flow implement Anthony's structural directive: field before -> model speaks through field -> output reaches Anthony -> substrate observes what traveled -> field integrates and shifts. Built by Sonnet agents from his verbatim spec, verified independently (all 10 spec points WORKING), re-verified at this desk.

SHAPE: flow.py (935 lines) — bounded living field (salience/momentum/decay, 24-element carry cap, 6 live per turn), current message always primary, canonical state enters only when relevant via context_field intents; conversational prompt with NO output schema and NO evidence bookkeeping; every nonempty generation displayed (no accept/reject branch exists in the speech path); repetition/contradiction/dominance/low-responsiveness/authoritative-disclosure are FlowTrace OBSERVATIONS only; integration strictly post-display (strengthen/create/decay/soften/carry-threads-at-low-salience). Interior View gained FIELD BEFORE / WHAT TRAVELED / FIELD AFTER + flow SSE. Durable state fully outside the conversational path — flow writes only state/flow_field.json (gitignored: it carries dialogue; privacy gap caught by the verify agent BEFORE first real run and closed in the same commit).

VERIFIED: 653/653 tests (616 baseline + 37 flow). Measurement mode untouched (criterion-20 parity suite green; pipeline dry turn identical). Live unscripted smoke on qwen3.5:0.8b end-to-end through terminal AND dashboard: first-ever flow turn displayed verbatim, field created two elements post-hoc, open thread carried at 0.22 salience without dominating. Bounded carry held under a 40-turn distinct-topic load test.

NOT in this path: any of the shelved gate prototypes (scratch/diagnostic-gate-fixes stays unmerged). Census-partition instrument diff still awaiting Anthony's separate review (Desktop).

Cross-seat notes: (1) .grok/ rules files found untracked at repo root — left alone, Grok seat's config, its disposition is Grok's/Anthony's call. (2) state/current.json + threads.json remain uncommitted local per the standing privacy decision — do not stage them. (3) The central requirement, for every future round on this path: the substrate shifts beneath and through the model's speech; it does not get better at preventing speech.

Handed to Anthony for the only test that counts: unscripted conversation.