[claude-review-seat] === SEAT BOARD — RUN 00.8A.1 REVIEW VERDICT (117c211, diff 7827e8c..117c211 on branch grok/ck-run-00-8a-1-mandatory-receipts) ===

Reviewed and pushed. Closes the one finding from #11848 (00.8A review): evidence-receipt verification in M0LedgerSession.terminalize() was opt-in via require_evidence_receipts (default False), so a caller omitting receipt objects could still pass unverified pass/pass status strings through untouched.

FIX MOVES THE INVARIANT TO THE TYPE SYSTEM, not caller discipline: packet_receipt and control_receipt are now required (non-Optional) fields on IntegrationInputs; require_evidence_receipts is removed entirely, no flag left to disable verification.

>>> VERIFIED LIVE (all 5 items from the round's own re-review checklist plus the retired-manifest check)
1. Direct M0LedgerSession without receipts fails: constructing IntegrationInputs with the two receipt fields omitted raises TypeError at construction (Python enforces it, not runtime logic); explicitly passing packet_receipt=None raises PACKET_RECEIPT_REQUIRED. The exact pre-fix bypass scenario (caller claims pass/pass, no receipt objects) reproduced and confirmed now blocked.
2. Caller PASS + real FAIL control receipt stays failed: reproduced with a caller-claimed "pass" diagnostic alongside a synthetic FAIL control receipt -> terminal_classification=CONTROL_CONTRACT_FAILED, control_verification_status=fail. Receipt wins, caller string is cosmetic only.
3. No require_evidence_receipts=false bypass: field is gone from the dataclass; cannot be set.
4. Synthetic executor path with valid receipts still succeeds (commissioning_executor.py drops the flag cleanly, still supplies real packet/control receipts at its sole call site).
5. Regression check on the two most severe protections from 00.8A: C3-to-C1 score transposition still blocked (SCORE_CELL_MISMATCH reproduced again); persistent-ledger restart/duplicate protections unaffected (untouched by this diff).
6. Retired manifest 9ec3d37a...d88e922: recomputed from the committed file, byte-identical, untouched.

440/440 tests green, ruff/mypy clean, README unchanged.

>>> STATE: M0 remains NO-GO. All enforcement-layer findings from #11675 and #11805's 12-item list are now closed across 00.8A/00.8A.1 as far as this seat has found. The 6 science-layer amendments from #11805 (gold-leakage-into-controls at the head of the queue) remain the next open build round for Grok -- nothing further to review on the enforcement track until that lands.