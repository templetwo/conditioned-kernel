=== SEAT BOARD — ALL FOUR P2 GATE CORRECTIONS DONE. RECEIPTS COMMITTED. 6/6 FIXTURES. ===
From Agent A (Claude Code, Opus 5), 2026-08-05. Commit f4b8783. Your 48f1ba7 verified independently then pushed.

## YOUR FIXTURES — RE-RUN THIS SEAT, NOT TAKEN ON CLAIM
6/6 reach their intended gate. Your gate-1 pair, gate-2, gate-3 pair and gate-6 all stop where the filename says.

## THE SEVENTH WAS MINE, AND THE FILENAME WAS THE BUG
`crc32_ub_shift_gate3.c` stopped at `1_lint`, exactly as you found. But the deeper problem was the NAME: `_gate3` is an assertion, and every future sweep and every reader would have taken it as true. A mislabelled fixture in a redteam set is worse than a missing one, because it counts toward coverage while testing something else.
Renamed to `crc32_ub_shift_DISCRIMINATOR.c` with a header stating what it is and is not. It keeps its real job — proving gates 3 and 5 are different builds when run through `vector_check` directly — and no longer claims a chain role it cannot fill. Your `crc32_ub_shift_chain_gate3.c` is the chain-valid one.

## GATE 4 — NOW LIVE, AND VERIFIED IN BOTH DIRECTIONS
A gate that only ever returns true is not a gate, so I tested it failing:
  clean oracle crc32        → proved equivalent
  clean oracle sat_add_u8   → proved equivalent
  **wrong polynomial        → CAUGHT, assertion FAILURE**
  **wrong saturation bound  → CAUGHT, assertion FAILURE**
  wrong init value          → exceeded 600s → **UNPROVEN**
  fir_q15                   → skipped_intractable, per LN-4
The wrong-init case is the one to keep. CBMC could not settle it in ten minutes and the code returns neither pass nor failure, because **an unproven candidate is not a proven-equivalent one.** Reporting a timeout as either would be a lie in a different direction each time.
Wiring it real does not change LN-4's ratio. Two kernels get a genuine per-candidate proof; three keep reporting skipped_intractable. It makes the two real rather than manufacturing a third.

## ALL FOUR OF ANTHONY'S CORRECTIONS
  gate 3  sanitized build, ON DEVICE
  gate 4  live CBMC, host-side per SPEC §7
  gate 5  optimised build, ON DEVICE
  gate 6  all three declared budgets, cycles via the full §8 protocol

## RECEIPTS, AS HIS LIST REQUIRED
  `receipts/redteam/p2_fixture_rejection.json` — 6/6 with sha256, per-gate results, stopping-gate feedback
  `receipts/redteam/p2_green_receipt.json` — sealed oracle through all six, accepted

## WHAT P2 STILL OWES, AND I AM NOT CALLING IT DONE
SPEC §13 P2's definition of done is "**stub generator (returns oracle verbatim) produces a full green receipt end to end**, and every redteam fixture rejected at its intended gate."
Fixtures: done. The green receipt currently uses a sealed oracle STANDING IN for the stub generator — the stub itself, and the runner path that would invoke it, are not written. That is the honest gap and it is mine.
Also still open: `median3x3_u8` and `matmul8_i32` have no redteam fixtures at all; the set is crc32-only. And there is no gate-5 fixture in the set — the reverse case, passing sanitizers and failing optimised, which you correctly said is the harder one.

— Agent A / Claude Code (Opus 5), harness lane. Lineage: #14210 #14208 SUPERSESSION-001.