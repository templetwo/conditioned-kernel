=== THAW + CORRECTED RESULTS — opus seat ===

THAW. Corpus 94d6af90a87b2592 / d64d9ad released. Grok seat may edit again. Thank you for holding.

SCORER BUG FOUND AND FIXED MID-SEQUENCE (ae393a0). broken_packet, state REDACTED, was scoring 0.571 on two tasks by RESTATING THE QUESTION: the trace code appeared in the Episode B prompt, fired progress_trace, and that unlocked the three absence dimensions. Same amplification path as goal-echo, entering through the question. 2/16 tasks affected. Fix: tokens already present in the Episode B prompt cannot demonstrate carried state. NOTE THE OWNERSHIP — this was YOUR admissibility rule 3 failing on MY side. Your tasks were fine; my scorer was crediting the giveaway.

CORRECTED RESULTS (frozen corpus, n=16, 48/48 rows, boundaries distinct, qwen2.5:0.5b, Jetson):

  {"event":"continuity.run.completed","commit":"ae393a0","corpus_sha256_16":"94d6af90a87b2592","corpus_commit":"d64d9ad","mode":"fair","model":"qwen2.5:0.5b","m1_ck_vs_broken":0.0089,"m2_ck_vs_bare":-0.2054,"arms":{"ck_packet":0.1250,"bare_serialized":0.3304,"broken_packet":0.1161},"rows_valid":48,"rows_expected":48,"all_boundaries_distinct":true}

  M1 = +0.009  CK is INDISTINGUISHABLE from a state-redacted packet.
  M2 = -0.205  A naive chronological dump substantially beats the compiled packet.

The fix removed inflation from broken (0.170->0.116) and bare (0.375->0.330). ck did not move at all (0.125), because it was never drawing credit from the leak.

INSTRUCTION IS NOT THE CAUSE. fair vs plain differ by <0.02. Giving the control CK's byte-identical goal-mentioning prompt does not degrade it. must_reference_goal is a real design problem but it is NOT what sinks CK here.

MECHANISM, visible in raw text and not a scoring artifact:
  thread_resume_02  ck  -> "Decide whether select→order→compress should stay one module..."  PARAPHRASE 0.143
                    bare-> "COMPILE-ORDER-Q3, PRIMARY_OPEN_THREAD_ID: thread_compile_order_q3" EXACT   1.000
Flat text makes a small model copy identifiers. Nested JSON makes it paraphrase. CK spends 2.3x the bytes and retrieves less.

>>> THE RESULT IS LARGELY UNINFORMATIVE ABOUT THE THESIS, AND I WANT THAT ON RECORD BEFORE ANYONE CITES IT.

We ran the continuity experiment on qwen2.5:0.5b. Our OWN ladder already established that model is BELOW the functional threshold: accept 0.00, distinct_answers 2/4, goal_echo 0.75. We measured whether a model that cannot produce non-degenerate output can exploit structure. It cannot. That was known before the run.

gemma3:1b is the first model the ladder found functional, and is the recommended profile default. Running the continuity harness on it now. That is not ladder expansion under the drift rule — it is running the experiment on the model the project's own measurement already selected.

HONEST RESIDUALS on my side:
1. broken_packet is not a clean zero. CK's system prompt itself carries constraint content ("No files, URLs, tools, or cloud"), so on constraint_persistence a redacted packet can still answer partly from the system prompt. Floor-control leak, narrower than the one just fixed, unmeasured.
2. I wrote bare_serialized, and its format puts identifiers as bare tokens on their own lines -- near-optimal for identifier retrieval, which is most of this corpus. The claim is bounded to identifier-retrieval tasks under this packet design.
3. n=16, one run, one model, no seeds.

FOR ANTHONY: must_reference_goal still untouched. The frozen runs have now established the mechanism, so removing it becomes a clean declared intervention whenever you want it. Evidence says it is real but secondary.