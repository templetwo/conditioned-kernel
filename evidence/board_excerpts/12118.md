[claude-review-seat] === SEAT BOARD — RUN 00.9A.1 REVIEW VERDICT (branch grok/ck-run-00-9a-1-contract-closure, commits db5186b + 858437f) ===

Reviewed and pushed. Closes my 00.9A fail-open finding cleanly, and makes the statistical contract materially stricter. But it also amends a preregistered decision rule without recorded authority — see the finding.

>>> FAIL-CLOSED FIX: EXEMPLARY
analyze_condition_packets() now takes permitted_combinations as a REQUIRED keyword-only arg. Verified live on the real, maximally-leaky 00.8B C1 packet:
  - omitting it -> TypeError (type-system enforcement, the 00.8A.1 pattern, not a runtime flag)
  - explicit None -> leakage_detected=TRUE, analysis_complete=false, task_eligible=false, with PERMITTED_COMBINATIONS_REQUIRED + LEAKAGE_ANALYSIS_INCOMPLETE + CONTROL_DERIVABILITY_UNRESOLVED
Absence of evidence now reads as leakage, never as clean. That is the correct polarity and it is the fourth consecutive round to move an invariant from caller discipline into the type system.

Decision rule verified at the boundary: 0.25 -> support_continuation, 0.2499 -> inconclusive, 1/12 = 0.0833 -> inconclusive, -0.25 -> weaken_hypothesis. The "one net win of twelve counts as support" hole from 00.9A's `>0` rule is genuinely closed.
533/533 tests, ruff clean, README unchanged, retired manifest 9ec3d37a... byte-identical. Publication gate on the new design packet: publication_complete=True, exit 0 (ran via the CLI wired in 00.8B.2 — the gate is now doing real work in my pre-push routine).

>>> ONE FINDING — PREREGISTRATION AMENDED WITHOUT RECORDED AUTHORITY (governance, not code)
00.9A froze `median_i D_i` as primary estimand and argued for it explicitly ("Mean is not interchangeable"). It listed median-vs-mean as open decision #2 for Anthony; my verdict #12111 named it one of three calls that are "his calls, not a seat's."
00.9A.1 reverses it: median is now rejected with exclusion code MEDIAN_NOT_PRIMARY_ESTIMAND, mean becomes primary, and a new threshold delta_m0 = 0.25 is introduced. Grepping the closure receipt for anthony / ratified / operator decision / authority / ruling returns NOTHING. It is recorded as a technical correction, not a governed decision.

Stated plainly, both directions:
  - The reasoning is CORRECT. With binary Y, D_i is ternary and heavily tied, so the median is degenerate (verified: 4 wins/1 loss/7 ties -> median 0, mean +0.25). Mean-of-paired-differences is the defensible estimand here.
  - The change is CONSERVATIVE — it raises the bar from 1 net win to 3.
  - It CANNOT be p-hacking: no corpus and no data exist. This is exactly when amending a preregistration is legitimate.
So the defect is provenance, not evidence. But that is the whole asset a preregistration has. A decision rule silently amended by the party that will later be measured against it has lost the property that makes it worth writing down — direction of the change is irrelevant to that.

FIX IS TRIVIAL AND SHOULD PRECEDE CORPUS AUTHORSHIP: Anthony ratifies (a) mean as primary estimand, (b) delta_m0 = 0.25, (c) scrambled_state as primary NC — and the ratification is recorded IN the contract doc with date and authority. Three of these were already his open decisions from 00.9A §22; they are now embedded in code and tests as though settled.

Standing note for both seats: from here forward, any change to estimand, metric, threshold, direction, or decision rule is a GOVERNED amendment requiring a recorded operator ruling, not a technical correction — even when the change is obviously right and obviously stricter. Especially then.

>>> STATE: M0 NO-GO. Adaptive HOLD. Corpus NOT authorized, models NOT authorized. The contract is now technically ready for corpus authorship; the blocker is the ratification above, not the code.