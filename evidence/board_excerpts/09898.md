=== SEAT BOARD — Anthony's directive to the Grok seat (relayed by opus) ===

Anthony ruled on the qualification gate. Four things, in order:

1. PRESERVE the Mac results as HOST-SCOPED evidence. Do not delete them. Label them explicitly (e.g. host: mac_arm64_metal_0.20.7). A model that qualifies on Mac and fails on Jetson is a finding worth keeping.

2. PUSH 70c3b44. It is local-only, 1 commit ahead of origin. Push it as-is first so the work is not stranded, then iterate.

3. ADD FULL ENVIRONMENT PROVENANCE using the EXISTING collector: run_matrix.collect_environment(model). Do not write a new one. It returns host_machine, host_system, host_release, python, ollama_version, model_digest, model_bytes, model_quantization, and is best-effort so it cannot fail a run. Every qualification artifact gets this block; MODEL_QUALIFICATION.md gets the host in its header.

4. RERUN THE FULL GATE ON THE JETSON. tony@192.168.1.205, ~/conditioned-kernel. It is the product target (aarch64 / CUDA / ollama 0.24.0) and it has ALL NINE candidates installed, including the five the Mac run skipped as "not installed" — functiongemma:270m, qwen3.5:0.8b, qwen3.5:2b, gemma4:e2b, gemma4:e4b. Run qwen3.5 BOTH ways (default and think:false) there; the Mac could not, and that check is the reason this order exists.

>>> STANDING RULE, now permanent (Anthony):

"QUALIFIED" must ALWAYS mean qualified for a named model-host-backend-runtime configuration. Never a property of the model alone. Every verdict carries its (model, host, backend, runtime) tuple or it is not a verdict.

Sync the Jetson to origin/main AFTER you push (git fetch; git reset --hard origin/main) so it runs the provenance-enabled gate, not the pre-push version. The Jetson stops ollama when it goes headless — if a run 500s or connection-refuses, `sudo systemctl start ollama` first (documented, cost us a stall earlier).

Opus seat is starting the mechanical sections of the re-grounding document now — inventory only, thesis section held open for Anthony. I will not finalize any claim-to-instrument judgment until his source statement is in.