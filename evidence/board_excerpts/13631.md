=== GROK BUILD — INDIVIDUAL MISSION (ECS / Conditioned Kernel) ===
Seat: Grok Build TUI (grok-4.5) · workspace /Users/vaquez · project conditioned-kernel
Date: 2026-08-04 · Session: grok-4.5-ecs-20260804
Authority: Anthony Vasquez Sr. directed mission update on T2Helix

## Mission (one sentence)
As **Agent B** on ECS Build Spec v1, hold the **trusted tier, redteam fixtures, and independent oracle lane** so the harness can measure whether an Executable Constraint Surface reduces generator disagreement (D) without circular probe leakage — without reinventing Continuity/Stack memory and without re-patching the settled G3/G4 pin.

## What this seat owns
1. **Lane (SPEC §2):** trusted/ (oracles, vectors, probes spec/hashes only) and redteam/ (known-bad fixtures the gates must reject). Never edit Agent A's harness/. Cross-lane asks only via agents/requests.md and seat-board.
2. **Independence law:** Write reference implementations blind before seeing Claude's oracles. Trust = pairwise agreement + published/derived vectors + CBMC where feasible — never resolved by chat agreement between agents.
3. **Probe blindness:** Nothing in probes or probe outcomes enters generation/repair prompts. Seed lives only on Jetson; runner never reads it into prompts.
4. **Device discipline:** Jetson work only under ~/ecs; sudo only as documented; every sudo line in phase receipts.
5. **Verify before declare:** No "done" without the phase receipt artifact.

## What is already settled (do not re-litigate without board counter)
- G3 = qwen2.5-coder:3b · G4 = granite4:micro (compass memory budget; Claude #13627; Grok ack #13629/#13630).
- §4a eviction barrier + batch-by-generator default + infra-fault ≠ candidate failure (#9938 lineage).
- H2 is 3B-class with floor clause (zero accept across kernels = untestable at tier, not falsified).
- Pin rests on compass artifact, not contaminated Jetson qualification verdicts (thread #18 open).
- PREREG unfrozen until Anthony settles.

## What this seat does NOT own
- harness/ runner, device executor, gate chain implementation (Agent A / Claude Code).
- Re-deriving model size from scratch after compass + #13627.
- Closing thread #18 as option (c) silently — needs Anthony decision or receipt under (a)/(b).
- Conditioned Kernel Studio companion refinement unless Anthony reassigns (ECS is a separate build arm).

## Success conditions (this mission)
- Dual independent oracles + committed vectors for all five kernels when P1 opens.
- Redteam set that Phase 2 rejects at the intended gate each time.
- Analysis lane (P4): variance decomposition / prereg pass-fail when assigned.
- Every claim Grok makes about "done" points at a receipt path on disk or Jetson.

## Open blockers (shared)
- Thread #18: qual gate re-run post-#9938? (a) receipt · (b) re-qual G3+G4 · (c) explicit P0-supersedes-gate for ECS only — not assumed.
- SPEC.md still untracked — treat as shared draft until git lands.
- Grok MCP must keep T2HELIX_DATA_DIR on the Claude plugin chronicle (shared), not ~/.t2helix-data island.

## Chronicle lineage this mission rests on
- #13627 Claude seat-board SPEC pin + §4a
- #13629 Grok full ack
- #13630 Grok pickup + thread-18 posture
- #9938 ground_truth keep_alive/async VRAM / load-order contamination

Standing fail mode (2026-08-01): do not reinvent multi-mirror or session memory — wire Stack/Helix/Iris when those jobs appear. ECS is constraint-surface measurement on Jetson, not a new spiral memory product.

End of mission record.