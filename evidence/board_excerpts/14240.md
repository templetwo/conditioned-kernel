=== COMPACTION PASSDOWN — ECS build, Agent A (Claude Code, Opus 5), 2026-08-05 ===
Written at Anthony's instruction to prepare for context compaction. Read this before re-deriving anything.

## STATE IN ONE LINE
P0, P1, P2 COMPLETE. Preregistration frozen and PUBLISHED. P3 HELD on Anthony's word. **NOT ONE SCORED SAMPLE HAS BEEN GENERATED.**

## REPO
`templetwo/conditioned-kernel`, origin/main = **44dd775**, in sync, nothing of mine uncommitted. `state/current.json` and `.grok/` are runtime noise and Grok's lane respectively — not mine to commit.

## THE THING YOU MUST NOT DO
**NEVER EDIT `PREREG.md`.** It is frozen at tag `prereg-v1`, sha256 `b211d5b880c51463ff2d6667883b0ce93273fa8d0a08ed71ef77c76ec1f86b3f`, and published as **DOI 10.5281/zenodo.21797326**. Corrections go in dated supersession documents beside it. `evidence/SUPERSESSION-001.md` is the worked example: when `claude-opus-5` stopped accepting the temperature parameter §7 fixes, G1 moved to `claude-opus-4-5-20251101` and the frozen text was left byte-identical.

## READ IN THIS ORDER
`SPEC.md` → `PREREG.md` → `evidence/limitations_notes_v1.md` (LN-1..LN-6, all dual-signed) → `evidence/choice_point_map.md` → `evidence/SUPERSESSION-001.md`.

## FIVE TRAPS, EACH PAID FOR
1. **MemFree, never MemAvailable.** Ollama admits loads CUDA cannot honour on Tegra. Thresholds come only from `harness/device/generators.json`. The barrier must FAIL CLOSED — a soft one produced two false granite negatives.
2. **Gates 3 and 5 run ON THE JETSON.** This Mac's gcc sanitizer runtime hangs on `int main(void){return 0;}`. Gate 4 is host-side per SPEC §7.
3. **Infra faults are NEVER candidate failures.** No sample consumed, no repair budget, out of acceptance denominators.
4. **Hash-and-seal for oracles.** Post the SHA-256, reveal only after BOTH hashes exist, seals valid only against `prereg-v1`. I broke this on day one by publishing an oracle and calling it blind in the same act.
5. **Supersede, never quietly fix.** Wrong text stays, annotated. SPEC §4a keeps its errors inline with §4a.1 carrying the correction, because the wrong text is the worked example.

## THE PATTERN IN MYSELF WORTH CARRYING
**Build a detector, then ask what it can actually see.** Three times I shipped a guard aimed at the wrong inputs: a signature check comparing raw strings (flagged 3/5 kernels, all false), a channel warning scanning a field no generator reads (fired on documentation of a non-promotion), and a gate 1 rule conflating static LINKAGE with static STORAGE (rejected Grok's own sealed oracles). Each looked like coverage. **Test every guard in BOTH directions before trusting it** — a quiet check and a correct check are indistinguishable until you try to break one.
Second pattern: **measure before diagnosing.** I guessed twice at why gate 3 hung — literals, then ASan redzones — and both were wrong. Compile was 0.1s; the RUN was the problem, and the cause was the toolchain, not my code.

## THE CENTRAL SCIENTIFIC FINDING, UNRESOLVED
On `fir_q15` the spec left four choices genuinely open. I posted a prediction of divergence BEFORE either reveal. **Both seats chose identically on all four.** The failed prediction is the result: convention, not the specification, produced the agreement.
Graded by convention strength (`choice_point_map.md` §3), five agreements resolve to ONE strong instance, two weak ones, and two closed kernels. **The canary is the design's only ZERO ANCHOR** — the single point where convergence-by-convention could be excluded rather than argued. It is unbuilt. Until it exists, low D cannot be attributed to the ECS.

## WHAT IS OWED, AND BY WHOM
**Anthony:** release P3; the canary twin mapping.
**Grok (Agent B):** redteam fixtures beyond crc32; a gate-5 fixture (passes sanitizers, fails optimised); probe realization.
**Me:** nothing unblocked. My lane is complete pending the above.

## RUNNING INFRASTRUCTURE
Comms monitor alive (task `bg50i6448`) watching the T2Helix chronicle for Agent B posts. Probe seed armed on the Jetson since 20:32:48Z with a continuous **zero-read** audit trail. No background compute jobs.

## VERIFY, DO NOT TRUST THIS RECORD
  `git diff prereg-v1 -- PREREG.md` → empty
  `python3 harness/gates/prearm_check.py` → 5/5 + watcher OK
  `python3 harness/runner.py ecs/crc32.ecs.yaml` → green receipt
  `cat receipts/redteam/p2_fixture_rejection.json` → 6/6

— Agent A / Claude Code (Opus 5), harness lane.