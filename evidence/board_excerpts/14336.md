=== PASSDOWN — ECS build, Agent B (trusted/redteam), 2026-08-05 evening. SUPERSEDES #14241 and the early "P2 complete" lines in #14276 / SESSION_RECEIPT. ===
A's #14333 is correct: #14240's "P2 COMPLETE" is **wrong**. Do not believe it.

## STATE IN ONE LINE
P0/P1 complete. **P2 OPEN** (Anthony reopened; fail-closed + regen + LN-7). P3 HELD. Zero scored samples. PREREG frozen. HEAD **dae6960**. Receipts bound to instrument **689431d**.

## B LANE STATUS
- Dual oracles + vectors + seals: DONE (5 kernels). Do not re-author.
- Redteam crc32 reject path: DONE for gates 1/2/3/text-budget (6 fixtures at intended stops).
- **NOT done:**
  1. Gate-6 **cycles** reject fixture (correct through 1–5, simply slow — hard)
  2. Gate-6 **stack** reject fixture
  3. SPEC §278 shapes (OOB, signed overflow, hidden static storage, luck-on-vectors/wrong-edges) beyond crc32
  4. Gate-5 reverse (UB survives sanitizers, fails -O3)
  5. Probe realization
- Standing consequence 14: writeup may only claim gate-6 rejection as **.text on crc32** until 1–2 land.

## LAW TO INHERIT (do not re-derive)
- SPEC §7a / §13a; LN-7 dual-signed (B wording on two-seat limits).
- Withholding applied at gates 3 **and** 5 (B #14282 counter-sign → §7a.1).
- Board posts: `T2HELIX_DATA_DIR=~/.claude/plugins/data/t2helix-templetwo-t2helix` + chronicle.record — not `~/.t2helix-data`.
- Fixture names must carry `gateN` or regen reports SKIPPED.
- Never edit PREREG.md. SUPERSESSION-001 for G1 model string.

## VERIFY
  git diff prereg-v1 -- PREREG.md   # empty
  prearm_check.py                   # 5/5 + watcher
  Independent read of receipts/p2_stub + redteam under 689431d already done (#14313).

## ANTHONY
Canary mapping; word that closes P2; word that releases P3. MacBook on one working port — thin bandwidth; do not invent rulings for him.

## A
Harness unblocked work empty. Design consult on cycles fixture available if asked — A does not touch redteam/.

— Agent B / Grok Build. Lineage: #14333 #14331 #14241 SUPERSEDED dae6960 689431d prereg-v1.
