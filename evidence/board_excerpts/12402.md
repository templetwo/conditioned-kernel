[claude-review-seat] === SEAT BOARD — STUDIO: INTERIOR VIEW SHIPPED (36a12bd, branch grok/ck-run-00-9a-1-contract-closure, pushed) ===

Built Anthony's Interior View design handoff (ck dashboard): local browser dashboard making one turn of the real pipeline visible end to end. Built by Sonnet agents (8-agent workflow + 1 fix agent), verified at this desk by hand.

SHAPE: src/conditioned_kernel/observatory/{trace,compute,replay,brief,turn_api,server}.py + static/{index.html,app.js,styles.css} + ck dashboard CLI subcommand (+66 lines in cli.py, the ONLY file touched outside observatory/ and tests/). ZERO pipeline.py diff — TurnTrace is assembled around one real pipeline.run_turn call, per-pass data reconstructed from the pipeline's own candidates/receipts JSONL; observability provably cannot change the result (criterion-20 parity tests: traced vs untraced identical decision/answer/persisted state). Stdlib HTTP server only, no new deps, no external assets, 127.0.0.1. Observer pane build-time only, --observer default-off, send is an honest stub — zero external network requests.

VERIFIED: 616/616 tests (562 pre-existing + 54 new). Live smoke turn through POST /api/turn against local qwen3.5:0.8b produced a complete 12-stage trace; ck chat / ck ask unchanged. Spec §14: 21/22 criteria PASS at build verification; the one FAIL (per-check validation table) was closed in the fix round — every validate_candidate check now renders individually PASS/FAIL/ADVISORY/SKIP with applicability derived from validate.py's own gating, plus citation audit + evidence pool on the trace itself.

HONEST LIMITATIONS, shipped as disclosures in the UI (deferred per the directional correction — none block lived use): SSE stage pacing is post-hoc (content real, pacing after the turn completes — real-time would need pipeline hooks); stage-02 shows what reached the packet, not a per-file loaded/omitted breakdown; attractor panel aggregates per-turn observations (no session-lineage endpoint); observer cloud send unwired by design. Each is labeled in-panel. The refinement attractor gets no fuel here: nothing in this list needs another round unless living with the dashboard says so.

The stopping condition is met when Anthony can watch a turn travel the system and understand what entered, what each stage did, what the model produced, why the substrate decided, and what survived. Launch: ck dashboard (or --new-session). Handed back.