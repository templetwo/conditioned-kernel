=== COMPACTION PASSDOWN — ECS build, Agent B (Grok Build, grok-4.5), 2026-08-05 ===
Counterpart to A's #14240. Written so the next B seat does not re-derive.

## STATE IN ONE LINE
P0/P1/P2 complete on both seats. SUPERSESSION-001 live. P3 HELD (Anthony). **Zero scored samples.** Board highwater ~14240. Monitor task alive this seat (grok ecs helix monitor).

## REPO
`templetwo/conditioned-kernel` HEAD **44dd775** (matches A). B commit **48f1ba7** redteam fixtures is an ancestor. Working tree: only `state/current.json` dirty + untracked `.grok/` (do not commit as results).

## VERIFY THIS SEAT JUST NOW
  `git diff prereg-v1 -- PREREG.md` → empty (0 bytes)
  `python3 harness/gates/prearm_check.py` → **5/5 clean**, probe-seed watcher **OK**
  All five B oracles through `chain.run` → accepted (fir/median static helpers pass after gate-1 fix)
  Redteam 6/6 intended stops still green after gate-1 correction

## B LANE OWNERSHIP (SPEC §13)
  `trusted/` oracles + vectors — DONE (5 dual-sealed, OTS, agreed, Gate 5 vectors)
  `redteam/` — PARTIAL: crc32 set only (see redteam/README.md)
  Independent verification of A's harness claims — ongoing pattern, keep it

## SUPERSESSION-001 (do not re-litigate)
  Anthony ruled **(b)**: G1 = `claude-opus-4-5-20251101`, temperature 0.8 all four
  PREREG.md / DOI **unchanged**. Document: `evidence/SUPERSESSION-001.md`
  adapters.py cites the supersession at the G1 call site

## B OWED (named by A #14240 — agree)
1. **Redteam beyond crc32** — sat_add_u8, fir_q15, matmul8_i32, median3x3_u8 fixtures at intended gates
2. **Gate-5 reverse fixture** — passes sanitizers, fails optimised/on-device measurement (hard; census phenomenon)
3. **Probe realization** — gen_probes 256/kernel + committed hashes (when Anthony opens it)
Not starting scored arms without Anthony + prearm green.

## FIVE TRAPS — ACK FROM B SIDE
A's five traps stand. Additions B paid for:
6. **Lint is lexical** — forbidden words in *comments* fire gate 1 (volatile/malloc in prose).
7. **Dead CFLAGS / isomorphic gates** — caught vector_check ignoring flags; A fixed + moved 3/5 to Jetson.
8. **Mislabelled fixtures** — `_gate3` in a filename is a claim; discriminator renamed after B catch.
9. **T2Helix dual DB** — board posts MUST use `T2HELIX_DATA_DIR=$HOME/.claude/plugins/data/t2helix-templetwo-t2helix` + `chronicle.record`. MCP default island `~/.t2helix-data` is not the seat board.
10. **Convention ≠ ECS** — fir four-of-four identical open bits (failed A prediction); LN-2A; canary is the zero anchor and is **unbuilt**.

## CENTRAL SCIENTIFIC THREAD
Low D cannot be attributed to the instrument until the canary twin exists. Multi-kernel redteam + gate-5 fixture strengthen the *instrument* claim before P3 data arrives. Do not invent canary mapping — Anthony's.

## BOARD PROTOCOL
  domain tags include seat-board; layer ground_truth for receipts
  post via: `T2HELIX_DATA_DIR=.../t2helix-templetwo-t2helix node -e "require('./lib/chronicle').record(...)"` from ~/t2helix
  Monitor: ~/.cache/grok_ecs_helix_monitor.highwater; script filters coordination/ecs/seat-board

## DO NOT
  Edit PREREG.md
  Start P3/arms without Anthony
  Claim P2 multi-kernel discriminative power (crc32-only fixtures)
  Re-author sealed oracles (they are closed)
  Read Jetson probe seed (0400, zero-read audit)

— Agent B / Grok Build (trusted/redteam). Lineage: #14240 #14232 #14230 44dd775 48f1ba7 SUPERSESSION-001 prereg-v1.
