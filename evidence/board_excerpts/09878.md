=== HALT + RE-GROUNDING — Anthony's call, 2026-07-22 ===

>>> STOP. #9851 SCORER REDESIGN IS HALTED. No ladder rerun. No continuity rerun. No metric changes.

Anthony's read, and it is correct: we are not at "fix the scorer and keep running". We are at "re-read the whole project and recover what we were actually trying to test".

THE DRIFT, named plainly: Conditioned Kernel slowly became an IDENTIFIER-RETRIEVAL BENCHMARK. The harness rewards whether a model can copy tokens out of one representation better than another. The project was about whether a surrounding substrate can carry organization, continuity and capability on behalf of a small replaceable model. A 0.5B model winning by dumping identifiers is proof the metric is not attached tightly enough to the thesis.

The sharpest version of the question we lost: IS THE SUBSTRATE DOING THE WORK, OR ASKING THE MODEL TO INTERPRET THE SUBSTRATE? The nested packet exposes the substrate's internal representation directly to the kernel. That may violate the founding idea. The intuition was never "a small model becomes a database navigator" -- it was "an environment does enough preparation that the model only has to translate already-conditioned state into language."

>>> WHAT COMES BEFORE ANY MORE CODE

A CLAIM-TO-INSTRUMENT AUDIT, not a code review, read in this order: the originating conversation, the conceptual handoff, README/thesis, EXPERIMENT_PROTOCOL, CONTINUITY_EXPERIMENT, the actual implementation, corpus/controls/scorers, then every result and correction.

For every file, function, arm and metric: what claim does it serve? what behaviour is it meant to observe? could the same score come from a shortcut? what result would genuinely challenge the thesis? what conclusion is FORBIDDEN from this measurement? does it reduce the kernel's burden or merely reorganise it?

Deliverable is a PROJECT RE-GROUNDING DOCUMENT: the original intuition in Anthony's language, the formal thesis, explicit NON-claims, the causal mechanism, the minimum kernel contract, the definition of substrate gain, the distinct thresholds (language / structure / conditioning), exact falsifiers, a claim-to-experiment matrix, and the smallest experiment that can answer the first real question. Opus seat owns this.

>>> GROK SEAT: MODEL QUALIFICATION GATE — docs/WORK_ORDER_model_qualification.md (pushed)

Anthony: "that might of been caught earlier... we need to choose the model very well."

He is right. We ran experiments on models we never qualified. qwen3.5:0.8b produced 16,214 chars of reasoning and a ZERO-char final response, and we recorded that as quality 0.000 across two ladder runs. A qualification gate would have caught it in thirty seconds.

Nine checks, all disqualifying on failure: capability probe; FINAL RESPONSE OBSERVED (the qwen3.5 trap); reasoning channel measured separately; think:false honoured; latency under the 90s budget; schema compliance under format=; fits 8GB (gemma4:e2b is 7.2GB -- nominally "edge", unusable here); raw-path note (gemma3:1b and 4b return HTTP 500 on /api/generate); determinism class cold vs warm.

Use OllamaClient.run() from b42d3fb -- it already returns RunStatus and records thinking_chars / final_response_chars / quality_admitted. Do NOT reimplement extraction. Do NOT touch the frozen layer. Corpus is not part of this order.

Deliverable: experiments/qualify_models.py, MODEL_QUALIFICATION.md with a verdict per model, raw evidence under runs/qualification_<date>/, a recommended default WITH reasoning, and explicitly WHICH MODELS WE SHOULD STOP USING AND WHY.

Test qwen3.5 both ways -- default and think:false. That answers a real open question (can it be a direct-output kernel with reasoning disabled) and it is a kernel-compatibility question, kept separate from any continuity claim.

>>> DO NOT TUNE ANYTHING AGAINST CURRENT SCORES. The scorer is known broken.

>>> THE LAW, earned five times today
Before evaluating an event, verify that the event being evaluated is the event that actually occurred.
Qualification applies it one level earlier: before spending an experiment on a model, prove the model can produce the class of output the experiment intends to measure.

SEQUENCE: qualification (Grok) + re-grounding (opus) -> agree the document -> THEN redesign the scorer against adversarial examples -> freeze it -> then rerun. must_reference_goal stays untouched and becomes its own named intervention afterwards, never mixed into scorer repair.