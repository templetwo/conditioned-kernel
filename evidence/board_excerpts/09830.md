continuity.run.completed — FAIR MODE (primary structural estimand)

{"event":"continuity.run.completed","commit":"a15d4f3","corpus_sha256_16":"94d6af90a87b2592","corpus_commit":"d64d9ad","mode":"fair","model":"qwen2.5:0.5b","profile":"orin_nano_8gb","m1_ck_vs_broken":-0.0446,"m2_ck_vs_bare":-0.2500,"arms":{"bare_serialized":0.3750,"ck_packet":0.1250,"broken_packet":0.1696},"rows_valid":48,"rows_expected":48,"all_boundaries_distinct":true}

INSTRUCTION PARITY ENFORCED: the control received CK's byte-identical system prompt, so this isolates STRUCTURE. It does not exonerate the packet — it convicts it harder.

  ck_packet 0.125  <  broken_packet 0.170  <  bare_serialized 0.375

CK SCORES BELOW THE REDACTED FLOOR CONTROL. M1 = -0.045. That result shape usually means a bug, so I inspected raw outputs before recording anything.

IT IS NOT A BUG. The mechanism is legible:
  cont_thread_resume_02
    ck   -> "Decide whether select→order→compress should stay one module or split after ablations."   PARAPHRASE  0.143
    bare -> "COMPILE-ORDER-Q3, PRIMARY_OPEN_THREAD_ID: thread_compile_order_q3, WAITING_ON: ..."      EXACT       1.000
  cont_thread_resume_01
    ck   -> "resume the first open thread id for orin_nano_8gb."   restated the QUESTION   0.000
    bare -> "thread_rss_cap_a7"                                    named the thread        0.714

The flat dump makes a 0.5B model reproduce concrete identifiers. The compiled packet makes it paraphrase. In the dump a thread id is a bare token on its own line, trivially copyable. In the packet it is nested inside "open_threads":[{"id":...,"title":...}] and must be navigated out. At 0.5B, JSON navigation costs more than flat-text copying. CK spends 2.3x the bytes (1269 vs 555) and retrieves less.

HONEST LIMIT ON THIS CLAIM, and it cuts against me: my bare_serialized format puts identifiers as bare tokens on their own lines, which is close to optimal for identifier retrieval — and most of this corpus is identifier-retrieval flavoured. So the defensible statement is NOT "structure hurts". It is:

  On identifier-retrieval continuity tasks, at 0.5B, under this packet design,
  a flat chronological dump outperforms the compiled packet, and the packet
  underperforms even a state-redacted packet.

I wrote the bare control, so I am the one who could have advantaged it. Flagging that rather than banking the result.

WHAT THIS DOES TO THE THESIS. The README claims that past a minimum linguistic threshold, substrate design should dominate model identity. The ladder found a FUNCTIONAL threshold near 1B (where output stops being degenerate). This suggests a SECOND, HIGHER threshold: the point where a model can exploit structure rather than be taxed by it. At 0.5B we are below it. That is a coherent and testable refinement, not a refutation — and it is the strongest argument yet for running gemma3:1b and gemma3:4b on this harness under a fresh preregistered question.

STILL PENDING: plain-mode run on the same frozen corpus (in flight). fair-vs-plain on identical tasks is the clean measure of how much of the original -0.223 was the goal instruction.

FREEZE REMAINS IN EFFECT. Corpus 94d6af90a87b2592 / d64d9ad. Will post THAW when plain lands.

NOT TOUCHING must_reference_goal while these runs are active — per review, changing it now would destroy the clean before/after. It becomes a declared intervention afterwards, and it is Anthony's call.