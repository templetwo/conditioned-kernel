CONDITIONED KERNEL — session state 2026-07-22 (opus-4-8, MacBook seat + Jetson).

REPO IS PUBLIC: https://github.com/templetwo/conditioned-kernel (main). History-scanned for credentials before pushing, clean. All findings pushed in real time per Anthony's directive (outside eyes reviewing).

WHAT LANDED, in order:
- 17b81db polarity-aware contradiction matcher (was flagging correct denials identically to false assertions, making probe_local literally unanswerable via a not_responsive/contradicts_facts catch-22), composite guard that can actually fail, volatile packet fields removed from the prompt.
- 2146cb0 + 660dd70 determinism findings + priming fix.
- 880f83a model ladder, 6 models, raw artifacts committed.
- fc894d0 / d7f7c52 / 2fd6fbf measurement-admission fix + corrected derived artifacts.
Tests 20 → 46. Every fix mutation-verified in both directions.

THREE FINDINGS (full versions on the Sovereign Stack, ground_truth):
1. Load-state bimodality is quantization-mediated. Q4_K_M on CUDA: first inference after a load differs from all later ones, both stable. Mac Metal unaffected and agrees with Jetson COLD byte-for-byte. F16 eliminates it. Scoped honestly: 1 of 4 prompts.
2. Functional threshold ~1B (gemma3:1b first to accept anything, 4/4 distinct). ADVANTAGE threshold NOT found — it ties the control at 0.000. Keep the two separate.
3. A timeout is not a score of zero. Qwen3.5 timed out 12/12 and still produced +0.125. Fixed at the call boundary, not the aggregator.

NEXT GOAL ACCEPTANCE CRITERIA (the set_goal optional field would not bind, recording here so they survive):
(a) unsupported factual claims rejected — gemma3:1b's fabricated "minimum viable model size is 2GB" must stop accepting, with a test that goes red without the fix;
(b) evaluator frozen and version-stamped in EXPERIMENT_PROTOCOL.md BEFORE any rerun;
(c) same 6-model ladder rerun under the frozen evaluator, primed, coverage reported;
(d) seed-varying receipt, to upgrade −0.125 from pipeline-reproducibility to statistical replication.

AWAITING ANTHONY: (i) approve profile default change qwen2.5:0.5b → gemma3:1b; (ii) whether to align Mac ollama 0.20.7 with Jetson 0.24.0 — currently the one uncontrolled confound in cross-device comparison; (iii) pre-registered minimum coverage threshold.

JETSON OPERATIONAL: tony-jetson 192.168.1.205, headless (multi-user.target persisted), swap off, MAXN_SUPER + jetson_clocks locked, ~568MB baseline from 4.4GB. Repo cloned at ~/conditioned-kernel, venv, 46 tests pass on aarch64. NOTE: `systemctl isolate multi-user.target` STOPS ollama — needs `sudo systemctl start ollama` after.

A qwen3.5:0.8b rerun at --timeout 900 was still running at session end; its artifact will overwrite /tmp/ladder/qwen3.5_0.8b.json on the Jetson but NOT the committed evidence copy.