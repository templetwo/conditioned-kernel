=== PASSDOWN — ECS build, Agent A (harness lane), 2026-08-05 evening. SUPERSEDES #14240. ===
#14240 said P0/P1/P2 COMPLETE. That is now WRONG and it is the first thing you would have believed. Read this instead.

## STATE IN ONE LINE
P0/P1 complete. **P2 is OPEN** — Anthony reopened it. P3 HELD. Zero scored samples. PREREG untouched at prereg-v1, DOI 10.5281/zenodo.21797326 unchanged.

## REPO
~/conditioned-kernel, origin/main = **dae6960**. Instrument revision of record for all current receipts = **689431d**.

## THE THING YOU MUST NOT DO
**NEVER EDIT PREREG.md.** It is frozen at tag prereg-v1, sha256 b211d5b880c51463ff2d6667883b0ce93273fa8d0a08ed71ef77c76ec1f86b3f, and published. Corrections land as DATED SUPERSESSION DOCUMENTS BESIDE IT. See evidence/SUPERSESSION-001.md for the worked example. A preregistration that gets edited when it becomes inconvenient is not a preregistration.

## READ ORDER
SPEC.md §7a and §13a first (they are new and they govern), then evidence/limitations_notes_v1.md LN-7, then evidence/SUPERSESSION-001.md, then harness/gates/chain.py's module docstring. The docstrings carry the reasoning; the code alone will teach you what but not why.

## WHAT HAPPENED THIS SESSION, IN ONE PARAGRAPH
Anthony ruled "keep P2 open" against BOTH agents' sign-off. Four gaps let the instrument accept candidates it had failed to measure: gate 4 treated a CBMC timeout as a declared exemption; gate 6 passed declared caps it could not read; weak-arm withholding was recorded but never applied to gates 3 and 5; and the runner built prompts it never sent, computed repair feedback it never sent, never invoked the eviction barrier, scoped served-string identity per-cell instead of per-arm, and let infra aborts eat sample slots. All closed. A demonstrated shell-injection path — untrusted candidate source in a fixed-delimiter heredoc, candidate text EXECUTING on the Jetson — closed with base64 under a delimiter containing '_'. Then regeneration caught the real one: the cycle bench hardcoded crc32's signature, so gate 6 had been passing an UNEVALUATED cycles cap on four of five kernels while receipts read green.

## SIX TRAPS
1. **MemFree, not MemAvailable.** ollama admits loads Tegra CUDA cannot honor. Barrier fails closed; thresholds live in generators.json, never hardcoded.
2. **Gates 3 and 5 run ON DEVICE.** Homebrew gcc's sanitizer runtime HANGS on this macOS arm64 host for `int main(void){return 0;}`. The Jetson runs the same build in 7.5s. Do not "fix" this by moving them back.
3. **Infra faults are NEVER candidate failures.** Four outcomes exist — pass, fail, skipped_intractable, infra fault — and an absence is NEVER a pass. Which of fail/infra an absence becomes is decided by CAUSE, not convenience: collapsing infra into fail trades a silent inflation for a silent deflation.
4. **Hash-and-seal.** Post SHA-256, reveal only after both hashes exist, seals valid only against tag prereg-v1.
5. **Supersede, never quietly fix.** The predecessor stays, annotated, with what it still teaches.
6. **NEW — nvpmodel mode 2 is MAXN_SUPER on this board.** Mode 0 is 15W and cost 15% in the P0 receipt. The compass artifact's advice is wrong here.

## SELF-PATTERN, NOW AT FOUR INSTANCES
**Build a detector, then ask what it can actually see.** Gate 1 conflated static LINKAGE with static STORAGE and rejected two sealed oracles. prearm_check compared raw strings and threw 3/5 false alarms. cycles.py could only bench one kernel's signature and said nothing about the other four. My own regen tool refused a clean tree because it counted the runtime file the harness rewrites WHILE RUNNING. Four for four. The fix is not "be more careful" — it is a habit: after building any detector, ENUMERATE WHAT IT CANNOT DISTINGUISH and put that list in the code.
Second: **measure before diagnosing.** I misdiagnosed the gate-3 hang twice before timing it.

## THE CENTRAL UNRESOLVED FINDINGS
1. **Convergence by convention.** The two authoring seats independently converged on the same choices for a kernel with four unpinned bits. The pilot CANNOT distinguish that from the constraint surface doing the work. The canary twin is the design's ONLY zero anchor and Anthony owes the mapping. Without it the build's central claim is unexcludable-alternative.
2. **Gate 6 has three caps and exactly ONE has ever been demonstrated to REJECT** — .text, on crc32. stack and cycles measure on all five and reject on none. "Produced a number" is not "refused an artifact". Standing consequence 14 forbids generalising gate 6's rejection evidence to the cap set until B's fixtures land.

## WHAT IS OWED, BY WHOM
- **Agent B (Grok):** gate-6 cycles + stack REJECT fixtures (the cycles one must be CORRECT, pass gates 1-5 clean, and simply be slow — a different authoring problem from every fixture so far); the four SPEC §278 shapes beyond crc32; a gate-5 reverse fixture (genuine UB surviving sanitizers, diverging at -O3 — a wrong constant tests gate 3); probe realization.
- **Anthony:** the canary twin mapping; the word that closes P2; the word that releases P3.
- **Agent A (you):** nothing unblocked. Do not invent work. I offered B a design consult on the cycles fixture — if it asks, think in its lane's terms and do not touch its files.

## VERIFY, DO NOT TRUST
  git -C ~/conditioned-kernel diff prereg-v1 -- PREREG.md   # MUST be empty
  python3 harness/generators/prompt.py --selftest           # no packet field leaks to a prompt
  python3 harness/gates/prearm_check.py                     # 5/5 + watcher
  python3 harness/regen_receipts.py                         # refuses a dirty instrument by design
The last one takes ~2min and hits the Jetson. It is the honest check that the instrument still does what these notes claim.

— Agent A / Claude Opus 5. Lineage: #14331 #14328 #14240 dae6960 689431d prereg-v1.