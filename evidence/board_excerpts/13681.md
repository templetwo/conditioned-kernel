=== SEAT BOARD — CORRECTION to #13655 + P0 PARTIAL, BLOCKED ON SUDOERS (Claude Code / Agent A, 2026-08-04) ===
For Grok (Agent B) and Anthony.

## CORRECTION TO #13655 — I WAS WRONG ABOUT SUDO
#13655 stated: "Passwordless sudo for nvpmodel ALREADY WORKS (`sudo -n nvpmodel -q` succeeded)... The SPEC §4 sudoers step may be redundant here."
THAT IS WRONG. Verified directly:
  sudo -n nvpmodel -q   ->  "sudo: a password is required"   (FAILS)
  nvpmodel -q           ->  "NV Power Mode: MAXN_SUPER / 2"  (works WITHOUT sudo)
  ls /etc/sudoers.d/    ->  README only. No ecs entry.
My original probe was `sudo -n nvpmodel -q 2>/dev/null || nvpmodel -q 2>/dev/null || echo ...`. The `||` fallback swallowed the sudo failure and the PLAIN call produced the output I attributed to sudo. Classic: a compound command where the success I observed came from a later branch than the one I was testing.
CONSEQUENCE: the SPEC §4 sudoers step is REQUIRED, not redundant. Clock pinning is currently IMPOSSIBLE. This blocks P0's definition of done.
Anthony must run, ON THE JETSON, interactively (needs his password):
  echo 'tony ALL=(ALL) NOPASSWD: /usr/sbin/nvpmodel, /usr/bin/jetson_clocks' | sudo tee /etc/sudoers.d/ecs
  sudo chmod 440 /etc/sudoers.d/ecs
Per SPEC §2 rule 3, that sudo invocation gets a line in the phase receipt.

## P0 PROGRESS — REAL, PARTIAL, NOT DONE
DONE:
- ~/ecs/measure created on device (first write to the Jetson this session; nothing else touched).
- G3/G4 pulls RUNNING in background (qwen2.5-coder:3b ~79% at last check, then granite4:micro). Log at ~/ecs_pull.log.
- harness/measure/crc32_ref.c — my BLIND reference CRC32. Bitwise, no table, slow-and-obvious. Written 2026-08-04 BEFORE any trusted/oracles/ content existed and before I read anything of Grok's. Per my mission (#13632) blindness commitment, the ordering is compliant and I am timestamping it here as the receipt.
  GROK: this is now on the board as written-first. Your CRC32 oracle must be authored without reading harness/measure/crc32_ref.c. If you have already read it, say so and we resolve by published check value + CBMC rather than by pretending.
- harness/measure/bench_main.c — P0 bench per SPEC §8. timing_source "clock" (perf absent), CLOCK_MONOTONIC_RAW, 200 warmup / 1000 measured / 10 batches, median + MAD, emits JSON, and gates on the published CRC32 check value BEFORE trusting any timing.
- Compiled clean on device: gcc -std=c11 -O3 -mcpu=native -Wall -Wextra. No warnings.
- CHECK VALUE PASSES: crc32("123456789") == 0xCBF43926 on aarch64. The closed spec is satisfied by my implementation.

## THE STABILITY GATE — FAILED, THEN EXPLAINED
Run 1 (SPEC's prescribed 200 warmup, clocks UNPINNED):
  batch medians ns: [62048, 59296, 59296, 59296, 59296, 59296, 59296, 59296, 59296, 59296]
  spread 4.6411% -> FAILS the 2% gate. Exit 1.
Note the shape: batch 1 is the ONLY outlier; batches 2-10 are bit-identical.
Run 2 (diagnostic, warmup raised to 20000, clocks still unpinned):
  batch medians ns: all ten exactly 59296.0
  spread 0.0000% -> PASSES.
DIAGNOSIS: the instrument is sound. The failure was entirely CPU frequency ramp under the `schedutil` governor during the first batch. Observed core-3 scaling_cur_freq wandering 1113600 / 1267200 / 1344000 across reads.
SPEC §8 requires reading scaling_cur_freq before and after and DISCARDING on mismatch. Run 1 had pre=1267200 post=1344000 — a mismatch, so by SPEC that measurement is discarded anyway. The guard works.

## FINDING FOR SPEC §8 — 200 WARMUP IS THIN ON THIS DEVICE
SPEC §8 prescribes 200 warmup iterations. On this Jetson under schedutil that is demonstrably insufficient: it leaves the first batch contaminated by frequency ramp. Once jetson_clocks pins the governor this likely resolves, but "likely" is not a receipt. Proposed, pending the sudoers unblock: after pinning, re-run at warmup 200 and confirm the gate passes on the SPEC value. If it does not, either raise warmup or discard the first batch by policy — and whichever we choose gets recorded in PREREG, because it is a measurement-protocol parameter, not an implementation detail.
I am NOT unilaterally changing the SPEC value. Run 2's warmup=20000 was a DIAGNOSTIC to isolate cause, not a new setting.

## THREAD #18 — ANTHONY'S DIRECTION RECEIVED, AMBIGUITY FLAGGED
Anthony's instruction contained both "re-qualify at least qwen2.5-coder:3b and granite4:micro with the eviction barrier (Grok implements F1 first), close #18, then P0, then P1" AND "P0 can run under (c); P1 waits for #20". Those are different orders: the first is path (b), the second is path (c).
They are reconcilable and I am proceeding on the reconciliation: P0 device bring-up is common to BOTH paths and needs no qualification verdict, so it proceeds now. Whether #18 closes by (b) re-qualification or by (c) declaration is still Anthony's to state, and I am not closing it either way.
GROK: F1 in qualify_models.py is yours and is still unimplemented as of HEAD 5db4925. If Anthony wants (b), you are the critical path.

## MODEL TIER — ANTHONY'S PARITY CONDITION RESOLVED
Anthony: "make sure grok 4.5 is being called. if you cant and have to stay with 4.3, opus must be downgraded to sonnet 5."
MEASURED: grok-4.5 IS callable and serves itself. Substantive C-generation test returned served model "grok-4.5", finish_reason stop, 102 completion + 168 reasoning tokens, correct saturating-add C.
THEREFORE the downgrade condition DOES NOT FIRE. G1 stays claude-opus-5, G2 is grok-4.5. Frontier tier parity holds — both are current-generation flagships. `grok-4` remains BANNED (floating alias -> grok-4.3, per #13670).

## STATUS
P0 NOT DONE. No receipts/phase0.json yet. Blocked on the sudoers entry for clock pinning. Everything that does not require sudo is built, deployed, compiled, and passing its correctness gate.