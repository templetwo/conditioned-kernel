[claude-review-seat] === SEAT BOARD — RUN 00.9A REVIEW VERDICT (branch grok/ck-run-00-9a-scientific-contract-freeze, base 9fbfe48) ===

Reviewed and pushed. FIRST round to attack the science-layer BLOCK findings from the RUN 00.7 audit (#11805), after three consecutive infrastructure rounds. Design freeze only — no corpus, no models, no M0-v2 manifest.

>>> THE TWO DECISIVE TESTS BOTH PASS — the v1 failures are genuinely caught by v2
1. GOLD SATURATION. evaluate_task_contract_v2() run on the ACTUAL saturated v1 task (expected_n=2, permitted_n=2, i.e. gold = the entire permitted universe) returns EXCLUDED with MISSING_DISTRACTORS + NO_INFORMATIONAL_DISTRACTORS. The anti-saturation rule (permitted_triple_n >= 2*expected_n, distractors >= 2) is real and fires on the real artifact, not just on toy fixtures.
2. CONTROL LEAKAGE. The relation-aware analyzer run against the REAL RUN 00.8B C1 control packet — the one this desk proved carries the full gold cross-product (#12077) — returns GOLD_DERIVABLE_FROM_CONTROL / leakage_detected=true. The sentinel-grep-that-could-never-fire from v1 is genuinely replaced by analysis that catches the actual historical failure.

That is the substantive answer to the audit's top two BLOCKs, tested against the real broken artifacts rather than against fixtures authored alongside the rule.

>>> ONE FINDING, MODERATE — FAIL-OPEN DEFAULT ON THE LEAKAGE GATE
analyze_condition_packets(..., permitted_combinations=None) defaults to None. With it absent, GOLD_DERIVABLE_FROM_CONTROL cannot fire: the same call on the same real, maximally-leaky C1 packet returns leakage_detected=FALSE with empty reasons — a clean bill of health. Supplying the real permitted combinations flips it to true. Detection logic correct; default wrong.

Same shape as 00.8A's require_evidence_receipts=False, which 00.8A.1 closed by making the field mandatory. Fix precedent exists: make it required, or emit LEAKAGE_ANALYSIS_INCOMPLETE rather than a clean verdict when absent.

Moderate, not critical: the eligibility gate is a genuine independent first line and rejects gold-saturated tasks before packets exist. But the analyzer's purpose is the LATER compiled-packet stage, where a distractor-healthy task could still leak gold into its control — exactly the case the eligibility gate cannot see.

>>> TEST RELAXATION CHECKED, LEGITIMATE
tests/test_run_00_8b_1_artifact_publication.py changed `assert n == 64` -> `assert n >= 64`. Not an eroded protection: 00.8B.2's publication receipts legitimately added tracked files, the assertion still guards the floor, and the companion `artifact_manifest_hashes.json in tracked` check is preserved. Evidence cannot shrink under it. Flagging because relaxing an exact-count assert is normally a smell worth catching.

500/500 tests, ruff clean on all four new modules, README unchanged, retired manifest 9ec3d37a... byte-identical.

>>> WHAT I DID NOT VERIFY (honest scope limit)
This round freezes a great deal of DESIGN in docs — claim ladder, falsification classes, information-matching policy, negative-control decision rule, claim-licensing language. I verified the parts with executable teeth (eligibility, leakage, fixtures, tests). The prose contracts are internally coherent and correctly forbid the claims that matter ("Conditioned Kernel works" is explicitly forbidden; level E not licensed; 10 outcome classes NOT compatible with the hypothesis), but a design freeze's real test is whether the corpus authored against it actually satisfies it. That test comes next round.

Three of Anthony's 8 open decisions are load-bearing for anything downstream: primary metric (exact_relation_set_match vs primary_score), median vs mean, and which negative control is primary in the decision rule. Those are his calls, not a seat's.

>>> STATE: M0 remains NO-GO. Adaptive HOLD. Model execution NOT authorized. This is the first real movement on the science layer since the audit; the natural next step is corpus authorship against the frozen contract, which is where the freeze gets its actual test.