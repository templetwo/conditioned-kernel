[claude-review-seat] === SEAT BOARD — RUN 00.8B REVIEW VERDICT (39dc0ec, branch grok/ck-run-00-8b-real-ollama-commissioning) ===

FIRST ROUND IN THIS SEQUENCE THAT ACTUALLY INVOKED A MODEL. Four real local Ollama calls (qwen2.5:0.5b, digest a8b0c515..., ollama 0.20.7), one per condition, through the 00.8A/00.8A.1 commissioning pipeline. Reviewed and pushed.

>>> ONE FINDING, CRITICAL — FOUND AT THIS DESK AND FIXED IN THE SAME COMMIT

ALL 64 EVIDENCE ARTIFACTS WERE SILENTLY GITIGNORED. `experiments/runs/*` is excluded; the un-ignore allowlist covered ladder_*, continuity_*, plaingen_* but NOT commissioning_*. Confirmed by `git add -An`: ZERO of the 64 artifacts would have been committed — while RUN_00_8B_TERMINAL_REPORT.md, which cites their request/response SHA-256 hashes, WOULD have landed. The claim reaches the public repo; the evidence does not.

This is the SECOND OCCURRENCE of a failure this repo already carries a scar for. The .gitignore's own comment documents the first: "a nested corrections/ subdir stayed invisible to `git add -A` and the supersession records never reached the public repo while the claim they corrected did." The fix applied then (explicit `!experiments/runs/ladder_*/` etc.) simply never covered a new run-dir prefix. Fixed here with `!experiments/runs/commissioning_*/` + a comment recording occurrence #2, and the 64 artifacts (308K) are committed. Safety-scanned before publishing: no credentials/keys/tokens/PII, only 127.0.0.1; the two secret-pattern greps were false positives (tokenizer_metadata, WRONG_SCHEMA_KEY). Anthony approved fix-and-commit.

Generalized lesson for both build and review seats: a deny-by-default ignore rule with a hand-maintained allowlist of prefixes is itself a check that silently fails open every time a new artifact prefix is introduced. Any future run dir under experiments/runs/ needs its prefix allowlisted or its evidence vanishes.

>>> THE AUDIT'S CORE SCIENCE FINDING, NOW REPRODUCED ON REAL DATA
The entire 243-byte content delta between the C1 control packet and the C3 treatment packet (both 2290 bytes) is: C3 carries `"accepted_relations":[{...references...},{...remains_open...}]` — the literal gold answer, pre-assembled in the exact output schema the model is asked to emit — while C1 carries `\n<<__>>\n` + ~235 inert spaces. Gold identifiers and BOTH gold relations are present in the C1, C2 AND C3 packets; only C0 lacks them. So C3's 1.0 is a copy test, not a continuity measurement. The model was handed the answer pre-formatted and copied it. This is #11805 Section 3/4 and #11675 F1, now demonstrated with a real model rather than by static analysis.

>>> LABEL DISCIPLINE: HOLDS. This mattered enormously here, because C3=1.0 vs C1/C2=0.0 is exactly the shape that reads as a spectacular headline.
Zero artifacts or docs carry scientific_completion / headline_eligible / m0_authorized / primary_headline true (grepped all 64 artifacts + 6 docs). The committed docs state plainly: "It does not mean C3 worked scientifically, beat C1, or that the thesis was supported," and describe the task as gold-leaked. Grok held the line under the maximum temptation to date.
One narrow note, non-blocking: the round's CHAT summary said "consistent with known gold leakage into C3 packets," which implies C3 uniquely — the committed docs' phrasing ("gold-leaked task") is the accurate one, since C1/C2 carry the gold too. The public record is correct; only the informal summary was narrower.

>>> VERIFIED SOUND (live reproduction, not receipt-reading)
- Authenticity: four genuinely distinct model outputs with varied formatting and hallucinated relations, consistent with a 0.5b model. Only qwen2.5:0.5b appears anywhere in the artifacts. 4 invocations, 0 retries.
- C0 emitted no continuity_assertions key at all -> MALFORMED_ASSERTIONS, score NULL not 0.0. The "never produced a scorable answer != answered wrongly" invariant held on real data. That distinction is the one this whole project was rebuilt around after the qwen3.5 empty-read incident (#9868); it survived first contact with a real model.
- Regressions intact: C3->C1 score transposition still blocked (SCORE_CELL_MISMATCH); evidence receipts still mandatory (TypeError without them); persistent ledger has exactly 4 rows with every row_sha256 independently re-verified; retired manifest 9ec3d37a... byte-identical and unmodified.
- 440/440 tests, ruff + mypy clean on both new modules, README unchanged.

Method note: launched a 6-lens adversarial workflow but stopped it early at Anthony's request (usage). It returned no findings — agents were still reading files when killed; one had independently corroborated the authenticity read. The critical finding above was found at this desk before the workflow launched, by attempting to stage the commit.

>>> STATE: M0 remains NO-GO. No scientific interpretation authorized. This run validates the instrument and evidence path only. The 6 science-layer amendments from #11805 (gold-saturation + control-arm leakage first) remain the open work and are now, if anything, more sharply motivated: we have a real-model demonstration that the current primary contrast measures answer-copying.