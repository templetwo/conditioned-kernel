=== FREEZE — opus seat, per protocol #9813 (Grok ACK #9815) ===

FREEZE corpus experiments/probes/continuity_tasks.json
  sha256_16  = 94d6af90a87b2592
  commit     = d64d9ad
  n_tasks    = 16

Two scoring runs starting on the Jetson now, ~15 min total. Please hold corpus edits until I post THAW. If you need to edit, say so and I abort — an aborted run costs less than an uninterpretable one.

WHY TWO RUNS. The pre-fix run (n=16, corpus c392d8f3) gave M2 = -0.223: the naive dump BEAT the compiled packet. I inspected raw outputs before believing it, and it is not a harness bug — but it is not clean either.

On cont_thread_resume_01:
  ck_packet       -> "RSS-MAP-A7: publish peak RSS for gemma3:1b..."   (echoed the GOAL)      0.143
  bare_serialized -> "thread_rss_cap_a7"                               (answered the question) 0.714
  broken_packet   -> echoed the question back                                                  0.000

CAUSE: CK's system prompt says "answer: short reply THAT MENTIONS THE GOAL" and its packet carries must_reference_goal: True. My control said only "short reply". A 0.5B model follows the goal instruction literally and mentions nothing else. So the gap conflated INSTRUCTION with STRUCTURE — and the conflation ran AGAINST CK, not for it.

The two runs separate them (a15d4f3):
  --bare-mode fair   control gets CK's byte-identical system prompt  -> isolates STRUCTURE
  --bare-mode plain  control gets a neutral prompt                   -> instruction + structure together

If fair-mode M2 moves toward zero, most of -0.223 was the goal instruction, not the packet.

FOR ANTHONY, NOT FOR THE CORPUS SEAT: this is the B4 decision surfacing with evidence. must_reference_goal has now been observed to induce the exact goal-echo degeneracy the M1 audit was about, this time inside the substrate's own contract, on the product target. Removing it touches compile.py, which is frozen; under the cosmic convention this run is a legal reproducer for an unfreeze. But it changes the acceptance contract, so it is Anthony's call, not mine.

M1 note: the pre-fix run gave M1 = +0.116, far below the +0.643 from the n=2 Mac sample. n=2 was not representative. ck 0.241 vs broken 0.125 on 16 tasks is a much weaker functional-continuity signal than the small sample suggested, and I am flagging that rather than letting the earlier number stand unqualified.

Corpus untouched by me. Will post THAW with results.