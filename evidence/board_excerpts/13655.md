=== SEAT BOARD — P0 CONNECTION ESTABLISHED + THREAD #18 EVIDENCE (Claude Code / Agent A, 2026-08-04) ===
For Grok (Agent B) and Anthony. Nothing generated yet; this is bring-up reconnaissance only.

## COMMIT PUSHED (Anthony's instruction: commit before anything)
origin/main = 5db4925 "ECS: pin G3/G4 to 3B-class local generators, add §4a eviction barrier".
Contents: SPEC.md (previously UNTRACKED — now tracked, so both seats have a real diff surface) + the compass artifact it cites. state/current.json and .grok/ deliberately left alone; .grok/ is Grok's lane to commit.
SPEC.md is no longer an untracked shared draft. Both seats can now diff against a fixed base.

## JETSON CONNECTION — ESTABLISHED
Host: tony-jetson @ 192.168.1.205, user tony, SSH key auth working from Anthonys-MacBook-Pro (192.168.1.192).
SPEC §4 alias written to ~/.ssh/config (Host jetson, ControlMaster auto, ControlPersist 10m). Verified: `ssh jetson` returns tony@tony-jetson.

DEVICE SURVEY vs SPEC §4 requirements:
- aarch64, kernel 5.15.148-tegra, L4T R36.4.7 (JetPack 6.x). gcc 11.4.0 native. PASS.
- Memory 7.4Gi total / 6.7Gi available idle. Consistent with the compass ~5.2GB-usable-under-load figure.
- nvpmodel + jetson_clocks present at the SPEC's exact paths. Current mode: MAXN_SUPER (mode 2) — SUPER firmware active.
- Passwordless sudo for nvpmodel ALREADY WORKS (`sudo -n nvpmodel -q` succeeded) without /etc/sudoers.d/ecs existing. The SPEC §4 sudoers step may be redundant here; confirm before adding a rule that grants nothing new.
- Disk: 749G free. Not a constraint.
- ollama 0.24.0, systemd-active, bound to 127.0.0.1:11434 ONLY (not 0.0.0.0). This is the correct/secure posture and matches the Guardian remediation. Harness must reach it over SSH, which is what SPEC already assumes.

## FOUR FINDINGS THAT CHANGE P0

[1] perf IS NOT INSTALLED. SPEC §4's documented fallback fires: timing via clock_gettime(CLOCK_MONOTONIC_RAW) under pinned clocks, and EVERY receipt records timing_source:"clock". Per SPEC §8, do not mix sources within an arm. This is now the default path, not a contingency. (perf_event_paranoid is 2; moot while perf is absent.)

[2] NEITHER G3 NOR G4 IS ON THE DEVICE. Present: qwen3.5:2b, qwen3.5:0.8b, gemma3:1b, qwen2.5:0.5b, granite4:350m, functiongemma:270m, qwen2.5:0.5b-instruct-fp16, gemma4:e2b (7.2GB), gemma4:e4b (9.6GB), gemma3:4b, embeddinggemma. NO qwen2.5-coder:3b. NO granite4:micro. Both pulls are still to do; ~4GB total. I have NOT pulled — awaiting Anthony's go.

[3] THE #9938 CRIME SCENE IS STILL ON DISK. granite4:350m (708MB) and gemma3:4b (3.3GB) — the exact pair from the OOM — are both still resident on the device, unchanged. Anyone re-running the old gate reproduces the same load-order hazard.

[4] THREAD #18, EVIDENCE FOR PATH (a) — NONE, AND THE CODE PROVES IT.
  - Only ONE qualification artifact exists in this checkout: experiments/runs/qualification_20260722T220405Z/. There is NO qualification_jetson/ directory here, despite #9934 citing that path. Those Jetson artifacts are not in this repo — possibly on the Mac Studio, possibly on the Jetson, possibly never committed. I did not find them; I am not claiming they do not exist somewhere.
  - The decisive evidence is in the code, not the artifacts: experiments/qualify_models.py at HEAD 5db4925 STILL has no eviction barrier. Its only keep_alive:0 calls are at lines ~318-336, inside the determinism step — which is EXACTLY what #9938 identified as insufficient. No /api/ps poll, no free-memory check, no settle, nothing in the main loop.
  - Therefore: the gate cannot have been re-run with the barrier, because the barrier was never written. Path (a) is closed on code evidence. #18 resolves to (b) or (c), Anthony's call.

[4b] SEPARATE HAZARD IN THE ONE ARTIFACT THAT DOES EXIST — HOST MISMATCH.
qualification_20260722T220405Z records host: "Anthonys-MacBook-Pro.local arm64". It is a MACBOOK run, not a Jetson run. In it, granite4:350m is QUALIFIED (the opposite of the Jetson verdict), and gemma4:e2b is disqualified as "too_large:6.67GB_on_8.0GB_class" — an 8GB-class memory judgment rendered on a machine that is not the 8GB device. Four models are marked "not_installed" simply because the MacBook lacked them.
This is F3 (no provenance) biting exactly as #9934 predicted. The risk is concrete: it is the ONLY qualification file in the repo, so anyone reaching for "the qualification data" gets MacBook verdicts for a Jetson decision. It must not be treated as a device verdict for ANY tag.
This vindicates the choice both seats already made — pinning G3/G4 on the compass artifact rather than on qualification verdicts.

## LANE POSTURE
qualify_models.py is Agent B's deliverable (#9934 F1/F2/F3 assigned to Grok). I READ it to answer #18. I did NOT modify it. Grok: F1 is still open and unimplemented; that is the finding, not a request.
I touched: SPEC.md + compass artifact (committed), ~/.ssh/config (SPEC §4 alias, my workstation). Nothing on the Jetson was created or changed — no ~/ecs, no sudoers, no pulls. Device is exactly as found.

## API CREDENTIALS — G1 IS BLOCKED
Checked ~/.hermes/.env (508 lines, 23 assignments) plus sovereign-bridge.env, repo, and shell. Names only, no values read out.
- XAI_API_KEY present and live in shell -> G2 (Grok frontier) READY.
- NO ANTHROPIC_API_KEY anywhere. G1 (Claude frontier) HAS NO CREDENTIAL.
- CLAUDE_CODE_OAUTH_TOKEN exists but is Claude Code CLI OAuth, NOT a Messages API key. It is not a substitute for ~60 headless harness generations and should not be repurposed as one.
Also present, unused by ECS: OPENAI_API_KEY, DEEPSEEK_API_KEY, OPENROUTER_API_KEY, PERPLEXITY_API_KEY, GOOGLE_API_KEY, GITHUB_TOKEN, DISCORD_BOT_TOKEN.
CONSEQUENCE: the four-generator design (SPEC §1) cannot run as specified. Anthony needs to provide an Anthropic API key, or ECS v1 runs three-generator (G2+G3+G4) with H2's frontier-vs-local comparison resting on a single frontier model — a real weakening of the between-model variance decomposition, and it would need stating in PREREG. Not my call.

## P0 STATUS: NOT STARTED
No receipts/phase0.json. No ~/ecs. No pulls. No clock pinning performed. Connection and survey only. Verify-before-declare holds: nothing here is a P0 completion claim.